{% extends "layout.html.twig" %}

{% block title %}Mon profil{% endblock %}

{% block titleH1 %}
Mon profil
{% endblock %}

{% block content %}
<div class="row row-profil">
        <h2> informations :</h2>
            <table class="table table-hover table-profil">
                <tbody>
                  <tr>
                    <td>Nom :</td>
                    <td>{{ user.surname }}</td>
                  </tr>
                  <tr>
                    <td>Prénom :</td>
                    <td>{{ user.name }}</td>
                  </tr>
                  <tr>
                    <td>Adresse :</td>
                    <td>{{ user.adress }}</td>
                  </tr>
                    <tr>
                    <td>Code Postal :</td>
                    <td>{{ user.postalCode }}</td>
                  </tr>
                    <tr>
                    <td>Ville :</td>
                    <td>{{ user.town }}</td>
                  </tr>
                    <tr>
                    <td>Adresse email :</td>
                    <td>{{ user.username }}</td>
                  </tr>
                </tbody>
            </table> 
            <a href="{{ path('modifProfil', {'id':user.id}) }}">Modifier profil</a>
</div>
{% endblock %}